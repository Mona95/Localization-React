const styles = (theme) => ({
  modalBody: {
    padding: 20,
  },
  errMsg: {
    color: "red",
    fontWeight: "bold",
    fontSize: 11,
    marginLeft: 10,
  },
  langSelector: {
    position: "absolute",
    right: 0,
    top: 0,
  },
});

export default styles;
