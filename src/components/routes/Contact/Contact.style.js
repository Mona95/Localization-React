import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  contentWrapper: {
    padding: "4%",
    textAlign: "center",
  },
  title: {
    fontWeight: "bold",
  },
}));

export default useStyles;
